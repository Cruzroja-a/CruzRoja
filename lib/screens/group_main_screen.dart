// Archivo eliminado
// Este archivo ha sido eliminado como parte de la limpieza del código.
// Se eliminan todas las referencias a grupos.
import 'package:flutter/material.dart';

// Esta pantalla es solo un placeholder, la lógica real está en main_screen.dart
import 'main_screen.dart';

class GroupMainScreen extends StatelessWidget {
  const GroupMainScreen({super.key, String? groupId, String? groupName});

  @override
  Widget build(BuildContext context) {
    return const PantallaPrincipal();
  }
}
